import { HighlightMoveDirective } from './highlight-move.directive';

describe('HighlightMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMoveDirective();
    expect(directive).toBeTruthy();
  });
});
