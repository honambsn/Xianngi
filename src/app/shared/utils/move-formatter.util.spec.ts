import { MoveFormatterUtil } from './move-formatter.util';

describe('MoveFormatterUtil', () => {
  it('should create an instance', () => {
    expect(new MoveFormatterUtil()).toBeTruthy();
  });
});
